# ternary operator, 三元运算符

# 三元运算符是一种简洁的条件表达式，它是一种if-else语句的简写形式。
# do something if condition else do something else

is_friend = True
chat_message = "message allowd" if is_friend else "not allowed"  # 这是一个表达式，会返回值。
print(chat_message)
